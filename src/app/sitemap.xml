// pages/sitemap.xml.js
export default function SiteMap() {
  return null;
}

export async function getServerSideProps({ res }) {
  const SITE_URL = 'https://mathieu-dubris.web.app';
  
  // Pages principales
  const pages = [
    { url: '', priority: 1.0, changefreq: 'daily' },
    { url: '/about', priority: 0.9, changefreq: 'monthly' },
    { url: '/services', priority: 0.9, changefreq: 'weekly' },
    { url: '/contact', priority: 0.8, changefreq: 'yearly' },
    { url: '/blog', priority: 0.8, changefreq: 'weekly' },
    // Ajoutez aussi des variantes avec des titres optimisés
  ];
  
  // Ajoutez la date de dernière modification réelle
  const lastmod = new Date().toISOString().split('T')[0];
  
  const sitemap = `<?xml version="1.0" encoding="UTF-8"?>
    <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
      ${pages.map(({ url, priority, changefreq }) => `
        <url>
          <loc>${SITE_URL}${url}</loc>
          <lastmod>${lastmod}</lastmod>
          <changefreq>${changefreq}</changefreq>
          <priority>${priority}</priority>
        </url>
      `).join('')}
    </urlset>
  `;
  
  res.setHeader('Content-Type', 'text/xml');
  res.setHeader('Cache-Control', 'public, s-maxage=86400, stale-while-revalidate');
  res.write(sitemap);
  res.end();
  
  return {
    props: {},
  };
}